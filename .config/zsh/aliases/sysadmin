alias tobash="sudo chsh $USER -s /usr/bin/bash && echo 'Now log out.'"
alias tozsh="sudo chsh $USER -s /usr/bin/zsh && echo 'Now log out.'"

alias getNICs='ifconfig | grep -i "[[:alnum:]]*\:\s" | awk -F: '{print $1}' | grep -v 'lo''

